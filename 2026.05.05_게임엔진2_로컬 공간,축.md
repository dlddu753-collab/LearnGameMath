# [2026.05.05] 로컬 공간과 로컬 축

## 핵심 개념
- 게임 콘텐츠에는 게임 오브젝트마다 월드 공간과 로컬 공간이라는 두 공간 개념이 존재하며, 게임 제작 과정에서는 주어진 두 개의 공간 정보를 적절하게 활용해야 한다.

## 수학적 개념
- 로컬 공간 : 물체의 정보를 담기 위해 부여한 공간
> 독립적으로 단일 메시 정보만 표시하는 공간
- 월드 공간 : 여러 물체를 모아 게임 콘텐츠를 만드는 데 사용되는 새로운 공간
> 게임 콘텐츠를 만들기 위해 다양한 물체를 배치하는 공간
- 로컬 축 : 물체를 기준으로 설정된 방향 정보
> 로컬 축 정보는 로컬 공간의 기저벡터와 동일한 값을 가진다.
> - 로컬 축 -> 로컬 축의 이름  
> $+X$ -> 라이트 벡터 (Right Vector)  
> $+Y$ -> 업벡터 (Up Vector)  
> $+Z$ -> 포워드벡터 (Forward Vector)
- 트랜스폼에서 각 $\theta$ 로 설정한 로컬 축의 값 
> $\vec{r} = (cos\theta, sin\theta) = (r_x,r_y)$  
> $\vec{u} = (-sin\theta, cos\theta) = (u_x,u_y)$  
> - 아핀 회전 변환행렬은 삼각함수 대신 로컬 축 벡터를 사용해 생성할 수 있다.
> $$R = \begin{bmatrix}r_x & u_x & 0 \\ r_y & u_y & 0 \\ 0 & 0 & 1\end{bmatrix}$$
> - 모델링 행렬도 마찬가지로 로컬 축 벡터를 사용해 계산할 수 있다.
> > $$M = \begin{bmatrix}r_x \cdot s_x & u_x \cdot s_y & t_x \\ r_y \cdot s_x& u_y \cdot s_y & t_y \\ 0 & 0 & 1\end{bmatrix}$$


## 게임 적용 

## 코드예제
```c++
class TransformComponent
{
public:
	TransformComponent() = default;

public:
	// 게임 로직에서 사용할 멤버 함수
    ...
    // 트랜스폼의 회전 값이 변경될 때마다 Update 함수를 호출해 로컬 축 값도 함께 갱신
	void SetRotation(float InDegree) { _Rotation = InDegree; Update(); }
	void AddRotation(float InDegree) { _Rotation += InDegree; Update(); }
    ...
    // 로컬 축 정보를 가져오는 함수는 const 서식을 붙여 읽기 전용으로 선언.
	const Vector2& GetLocalX() const { return _Right; }
	const Vector2& GetLocalY() const { return _Up; }

	// 행렬 생성 함수
	FORCEINLINE Matrix3x3 GetModelingMatrix() const;

private:
    // 회전이 발생하면 로컬 축 정보를 변경하는 Update함수
	FORCEINLINE void Update();

private:
    ...
    // 좌우 방향에 대한 로컬 축 정보 설정. 표준기저벡터 e1
	Vector2 _Right = Vector2::UnitX;
    // 상하 방향에 대한 로컬 축 정보 설정. 표준기저벡터 e2
	Vector2 _Up = Vector2::UnitY;
};

FORCEINLINE Matrix3x3 TransformComponent::GetModelingMatrix() const
{
	return Matrix3x3(
        // 행렬을 생성할 때 사용하는 벡터 인자는 모두 열벡를 사용.
		Vector3(_Scale.X * _Right.X, _Scale.X * _Right.Y, 0.f),
		Vector3(_Scale.Y * _Up.X, _Scale.Y * _Up.Y, 0.f),
		Vector3(_Position.X, _Position.Y, 1.f)
	);
}

FORCEINLINE void TransformComponent::Update()
{
    // _Rotation 변수에 저장된 각도 정보와 삼각함수를 사용해 두 로컬축 벡터를 갱신
	float sin, cos;
	Math::GetSinCos(sin, cos, _Rotation);

	_Right = Vector2(cos, sin);
	_Up = Vector2(-sin, cos);
}
```
