# [2026.05.02] 브레젠험 알고리즘

## 핵심 개념
- 컴퓨터에 선을 그리기 위해서는 데카르트 좌표계가 아닌 스크린 좌표계를 사용하여  **픽셀화** 과정을 거쳐서 화면에 출력될수 있도록 해야한다


## 수학적 개념
- 스크린 좌표계 : 데카르트 좌표계에서는 y축이 위쪽을 향하지만, 스크린 좌표계는 y축이 아래쪽을 향한다
> 1. 데카르트 좌표계는 빈틈이 없는 연속된 실수로 평명을 채움.  
> 2. 스크린 좌표계는 독립된 영역을 가지는 이산적인 정수를 사용한다. 
- 픽셀 : 스크린 좌표와 색상에 대응하는 화면의 구성 요소
- 픽셀화 : 벡터를 화면의 점으로 최종 표현하기 위해서는 실수로 표현된 벡터 좌표를 정수로 변환한 후 색상을 부여하는 과정.
- 팔분면 : 원의 대칭성을 이용하여 공간이나 평면을 특정 기준에 따라 8개의 영역으로 나눈 것을 의미합니다.
> 점 $(x, y)$ 하나만 계산하면 대칭성을 이용해 8개의 점을 동시에 찍을 수 있어 연산 효율이 8배 증가함  
> - 팔분면 그래프

| 순서 | 좌표 | 각도 범위 | 위치 설명 |
|:---:|:---:|:---:|:---|
| 1 | $(x, y)$ | $0^\circ \sim 45^\circ$ | 제1사분면 하단 |
| 2 | $(y, x)$ | $45^\circ \sim 90^\circ$ | 제1사분면 상단 (y=x 대칭) |
| 3 | $(-y, x)$ | $90^\circ \sim 135^\circ$ | 제2사분면 상단 |
| 4 | $(-x, y)$ | $135^\circ \sim 180^\circ$ | 제2사분면 하단 |
| 5 | $(-x, -y)$ | $180^\circ \sim 225^\circ$ | 제3사분면 하단 |
| 6 | $(-y, -x)$ | $225^\circ \sim 270^\circ$ | 제3사분면 상단 |
| 7 | $(y, -x)$ | $270^\circ \sim 315^\circ$ | 제4사분면 상단 |
| 8 | $(x, -y)$ | $315^\circ \sim 360^\circ$ | 제4사분면 하단 |

- 브레젠험 알고리즘(중점 알고리즘) : 컴퓨터 그래픽스에서 직설을 그리거나 원을 그릴때 사용하는 대표적이고 효율적인 알고리즘이다.
> - 현재 픽셀 $(x_0, y_0)$에서 다음 칸인 $x_0+1$로 이동할 때, $y_0$ 좌표를 그대로 유지할지($y_0$), 아니면 한 칸 올릴지($y_0+1$)를 결정해야 한다.  
> - 이를 판별하기 위해서 직선의 방석식 $y= ax + b$를 사용할 것이다 ($a = \frac{h}{w}$, $w$=선분의 너비, $h$] =선분의 높이)
> 1. $y_0 = \frac{h}{w}x_0+b$ : 시작점 좌푯값 $(x_0,y_0)$
> 2. $y = \frac{h}{w}x + y_0 - \frac{h}{w}x_0$ : b값을 미리 계산하기 위해서 $b=y_0-\frac{h}{w}x_0$ 대입.
> 3. $\frac{h}{w}x + y_0 -\frac{h}{w}x_0 < y_0 + 0.5$ : y값을 중점의 y값($y_0+0.5$)과 비교  
-> 중점의 y값보다 작다면 : 수평으로 이동해 픽셀을 찍는다.  
-> 중점의 y값보다 크다면 : 수평으로 이동후 한 칸 아래로 내려 찍는다
> - 판별식을 단순하게 정리  
> 1. $\frac{h}{w}x + y_0 -\frac{h}{w}x_0 < y_0 + 0.5$
> 2. $hx + wy_0 - hx_0 < wy_0 + 0.5w$ : 양변에 $w$를 곱해서 분수 형태 제거
> 3. $h(x - x_0) - 0.5w < 0$ : 우변을 좌변으로 넘기고 $h$으로 묶음
> 4. $2h(x-x_0)-w < 0$ : 모든 항을 정수로 만들기 위해 양변에 2를 곱함
> 5. $2h(x_0+1-x_0) - w < 0$ : $x$에 $x_0 +1$ 을 대입
> 6. $2h - w < 0$ : 선분의 너비와 높이만 사용한 수식으로 다음 픽셀위치를 계산할수 있음
> - 판별식 패턴
> 1. 두번째 픽셀이 평행 이동한 경우 : 판별식은 동일하게 전개되며 $x$값에 $x_0 + 2$를 대입할 뿐이다.  
> -> $2h(x_0 + 2 - x_0) - w < 0$  
> -> $4h - w < 0$ : 기존 판별식에 $2h$ 만큼 증분 연산
> 2. 두번째 픽셀이 평행 이동 후 y축으로 내려간 경우 :  
> -> $\frac{h}{x}x + y_0 - \frac{h}{w}x_0 < y_0 + 1.5$  
> -> $hx + wy_0 - hx_0 < wy_0 + 1.5w$  
> -> $h(x - x_0) - 1.5w < 0$  
> -> $2h(x-x_0)-3w < 0$  
> -> $2h(x_0 + 2 - x_0) - 3w < 0$  
> -> $4h-3w < 0$ : 기존 판별식에서 $2h$ 만큼 증분 연산,$2w$만큼 증감 연산.
> 3. **픽셀을 찍기 위한 $x$값이 증가할수록 판별식 값은 언제나 $2h$만큼 증가, $y$값이 증가할수록 판별식은 $2w$만큼 줄어드는 패턴을 가지게 된다**
> - 팔분면에따라 판별식 변화
> 1. 두 번째 팔분면은 첫 번째 팔분면에서 $x,y$가 변되었을 뿐 전개 방식은 동일하다.  
> -> 두 번째 픽셀 : $2w - h$  
> -> 세 번째 픽셀 : $4w-h, 4w - 3h$  
> -> 네 번째 픽셀 : $6w - h,6w-3h,6w-5h$


## 게임 적용 
- 엔진이나 저수준 그래픽 라이브러리엣 직접 픽셀을 제어할때 브레젠험 알고리즘을 사용하여 효과적으로 구현할수 있다.
 ## 코드예제
```c++
void WindowsRSI::DrawLine(const Vector2& InStartPos, const Vector2& InEndPos, const LinearColor& InColor)
{
    // 시작과 끝점이 화면 영역을 벗어나는 경우 "클리핑 알고리즘"을 수행
	Vector2 clippedStart = InStartPos;
	Vector2 clippedEnd = InEndPos;
	Vector2 screenExtend = Vector2(_ScreenSize.X, _ScreenSize.Y) * 0.5f;
	Vector2 minScreen = -screenExtend;
	Vector2 maxScreen = screenExtend;
	if (!CohenSutherlandLineClip(clippedStart, clippedEnd, minScreen, maxScreen))
	{
		return;
	}
    // 시작점을 스크린 좌표계로 변환하고 startPosition변수에 저장
	ScreenPoint startPosition = ScreenPoint::ToScreenCoordinate(_ScreenSize, clippedStart);
     // 끝점을 스크린 좌표계로 변환하고 endPosition변수에 저장
	ScreenPoint endPosition = ScreenPoint::ToScreenCoordinate(_ScreenSize, clippedEnd);
    // endPosition X값에서 startPosition X 값을 빼 너비를 구하고 변수 width에 저장
	int width = endPosition.X - startPosition.X;
    // endPosition Y값에서 startPosition Y 값을 빼 높이를 구하고 변수 height에 저장
	int height = endPosition.Y - startPosition.Y;
    // 너비와 높이의 절댓값을 구하고 완만한 경사인지 급격한 경사인지 파악후 true or flase값을 저장
	bool isGradualSlope = (Math::Abs(width) >= Math::Abs(height));
    // x축 진행 방향이 양수인지 음수인지 판별
	int dx = (width >= 0) ? 1 : -1;
    // y축 진행 방향이 양수인지 음수인지 판별
	int dy = (height > 0) ? 1 : -1;
    // 변수dx와 선의 너비를 곱해서 변수에 저장. 판별식에 사용
	int fw = dx * width;
    // 변수dy와 선의 너비를 곱해서 변수에 저장. 판별식에 사용
	int fh = dy * height;
    // isGradualSlope 값에 따라 최초로 사용할 판별식 f를 지정
	int f = isGradualSlope ? fh * 2 - fw : 2 * fw - fh;
    // 선이 변환없이 진행되는 경우
	int f1 = isGradualSlope ? 2 * fh : 2 * fw;
    // 선의 진행에 변화가 생기는 경우
	int f2 = isGradualSlope ? 2 * (fh - fw) : 2 * (fw - fh);
    // 최초 선 그리기를 시작할 지점을 각x,y변수에 저장
	int x = startPosition.X;
	int y = startPosition.Y;
    // isGradualSlope값이 참이면 1,4,5,8팔분면의 선 그리기가 진행됨
	if (isGradualSlope)
	{
        // 끝 점에 도달할 때까지 로직을 반복
		while (x != endPosition.X)
		{
            // 지정된 색상으로 스크린 좌표에 점찍기
			SetPixel(ScreenPoint(x, y), InColor);
            // 판별식이 0보다 작으면 f1 값을 사용해 판별식 갱신
			if (f < 0)
			{
				f += f1;
			}
            // 판별식이 0보다 크거나 같으면 f2 값을 사용해 판별식 갱신
			else
			{
				f += f2;
                // 판별식이 0보다 크거나 같은 상황에서는 y축 값이 한 칸 진행
				y += dy;
			}
            // 다음 픽셀로 이동
			x += dx;
		}
	}
    // isGradualSlope 값이 거짓이면 2,3,6,7 팔분면의 선 그리기 진행. 로직은 동일
	else
	{
		while (y != endPosition.Y)
		{
			SetPixel(ScreenPoint(x, y), InColor);

			if (f < 0)
			{
				f += f1;
			}
			else
			{
				f += f2;
				x += dx;
			}

			y += dy;
		}
	}
}
```