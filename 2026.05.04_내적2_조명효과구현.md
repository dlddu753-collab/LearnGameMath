# [2026.05.04] 내적을 활용한 조명 모델의 구현

## 핵심 개념
- 두 벡터의 내적을 사용하여 사잇각을 구해 램버트 반사 모델을 사용하여 게임 제작에 필요한 실시간 조명 효과를 구현할수 있다.


## 수학적 개념
램버트 반사 모델 : 빛을 받아 표면에서 반사되는 빛의 세기는 두 벡터가 만드는 사잇각의 $cos$ 함수에 비례한다
> 표면이 향하는 단위 벡터 : $\hat{N}$  
> 표면에서 광원으로 향하는 단위 벡터 : $\hat{L}$  
> - 두 벡터의 내적을 사용하면 램버트 반사 모델에 필요한 사잇각 $cos$값을 얻을수 있다.  
> -> $\hat{N} \cdot \hat{L} = cos\theta$  
> 1. 내적의 결과값이 0일 때 : 표면과 광원이 수직 (밝기0)  
> 2. 내적의 결과값이 1일 때 : 표면과 광원이 일직선 (최대 밝기)
- 투영 벡터 : 하나의 벡터를 다른 벡터 방향으로 수직으로 내리꽂았을 때 생기는 그림자 벡터
> 1. 삼각함수와 내적 : $\vec{a} \cdot \vec{b} = |\vec{a}||\vec{b}|\cos\theta$ =$\cos\theta = \frac{\vec{a} \cdot \vec{b}}{|\vec{a}||\vec{b}|}$   
> 2. 투영된 길이 : $|\vec{a}|\cos\theta$ = $ |\vec{a}| \frac{\vec{a} \cdot \vec{b}}{|\vec{a}||\vec{b}|} = \frac{\vec{a} \cdot \vec{b}}{|\vec{b}|}$  <br>
> 3. 투영 벡터 : $proj_{\vec{b}}\vec{a} = \left( \frac{\vec{a} \cdot \vec{b}}{|\vec{b}|} \right) \frac{\vec{b}}{|\vec{b}|} = \left( \frac{\vec{a} \cdot \vec{b}}{|\vec{b}|^2} \right) \vec{b}$  
> 4. $\vec{b}$가 이미 정규화된 상태(길이가 1) : $proj_{\vec{b}}\vec{a} = (\vec{a} \cdot \vec{b}) \vec{b}$
> 
## 게임 적용 
- 게임속 물체에 광원효과를 부여하여 좀더 사실적인 그래픽 연출을 표현 할수 있다.
## 코드예제
내적을 활용한 조명 모델 구현
```c++
	// 광원을 받는 구체의 모든 픽셀에 NdotL을 계산해 음영을 산출하고 이를 최종 색상에 반영
	for (auto const& v : circle)
	{
		// 내적을 사용해 cos 값을 구하기 위해 원점에서 해당 픽셀 방향으로 향하는 벡터 구하고 이를 정규화
		Vector2 n = (v - circlePosition).GetNormalize();
		// 해당 픽셀에서 광원으로 향하는 벡터를 구하고 이를 정규화
		Vector2 l = (lightPosition - v).GetNormalize();
		// 벡터 n,l을 내적한 결과를 변수에 저장하고, 음수 값은 0이 나오도록 Clamp함수 사용
		float shading = Math::Clamp(n.Dot(l), 0.f, 1.f);
		// 빛의 색상과 shading 값을 곱해 해당 픽셀에 적용
		r.DrawPoint(v, lightColor * shading);
	}
```
내적을 활용한 벡터의 투영
```c++
// 게임 로직과 렌더링 로직이 공유하는 변수
// 원점으로 부터 250 만큼 떨어진 점의 위치를 변수에 저장
Vector2 point(0.f, 250.f);
// 점이 투영할 선의 시작점 저장
Vector2 lineStart(-400.f, 0.f);
// 점이 투영할 선의 끝점 저장
Vector2 lineEnd(400.f, 0.f);

// 게임 로직을 담당하는 함수
void SoftRenderer::Update2D(float InDeltaSeconds)
{
	// 게임 로직에서 사용하는 모듈 내 주요 레퍼런스
	auto& g = Get2DGameEngine();
	const InputManager& input = g.GetInputManager();

	// 게임 로직의 로컬 변수
	static float duration = 6.f;
	static float elapsedTime = 0.f;
	static float currentDegree = 0.f;
	static float rotateSpeed = 180.f;
	static float distance = 250.f;
	// 선을 구성하는 두 점의 Y값을 변경하기 위해 [-200,200] 범위 내의 값으 뽑아주는 랜덤함수 선언.
	static std::random_device rd;
	static std::mt19937 mt(rd());
	static std::uniform_real_distribution<float> randomY(-200.f, 200.f);

	elapsedTime = Math::Clamp(elapsedTime + InDeltaSeconds, 0.f, duration);
	// 경과 시간이 지나면 시작점과 끝점을 랜덤함수를 사용해 변경
	if (elapsedTime == duration)
	{
		lineStart = Vector2(-400.f, randomY(mt));
		lineEnd = Vector2(400.f, randomY(mt));
		elapsedTime = 0.f;
	}

	// 시간에 따라 각속도 180º로 회전하도록 점의 위치를 계산
	currentDegree = Math::FMod(currentDegree + rotateSpeed * InDeltaSeconds, 360.f);
	float sin = 0.f;
	float cos = 0.f;
	Math::GetSinCos(sin, cos, currentDegree);
	point = Vector2(cos, sin) * distance;
}

// 렌더링 로직을 담당하는 함수
void SoftRenderer::Render2D()
{
	// 렌더링 로직에서 사용하는 모듈 내 주요 레퍼런스
	auto& r = GetRenderer();
	const auto& g = Get2DGameEngine();

	// 렌더링 로직의 로컬 변수
	static float radius = 5.f;
	static std::vector<Vector2> circle;

	if (circle.empty())
	{
		for (float x = -radius; x <= radius; ++x)
		{
			for (float y = -radius; y <= radius; ++y)
			{
				Vector2 target(x, y);
				float sizeSquared = target.SizeSquared();
				float rr = radius * radius;
				if (sizeSquared < rr)
				{
					circle.push_back(target);
				}
			}
		}
	}

	// 붉은 색으로 점 그리기
	for (auto const& v : circle)
	{
		r.DrawPoint(v + point, LinearColor::Red);
	}

	// 투영할 라인 그리기
	r.DrawLine(lineStart, lineEnd, LinearColor::Black);
	r.DrawLine(lineStart, point, LinearColor::Red);

	//투영된 위치와 거리 계산
	// 투영 공식을 사용하기 위해 선의 끝점에서 시작점을 빼 벡터를 생성하고 이를 정규화
	Vector2 unitV = (lineEnd - lineStart).GetNormalize();
	// 점의 위치에서 시작점을 뺀 벡터를 구하고 변수에 저장
	Vector2 u = point - lineStart;
	// 투영 공식을 사용해 투영 벡터 구하고 변수에 저장
	Vector2 projV = unitV * (u.Dot(unitV));
	// 시작점에서 투영한 점의 위치 계산
	Vector2 projectedPoint = lineStart + projV;
	// 최단 거리 구하기
	float distance = (projectedPoint - point).Size();

	// 투영된 점 그리기
	for (auto const& v : circle)
	{
		r.DrawPoint(v + projectedPoint, LinearColor::Magenta);
	}

	// 투영 라인 그리기
	r.DrawLine(projectedPoint, point, LinearColor::Gray);

	// 관련 데이터 화면 출력
	r.PushStatisticText(std::string("Point : ") + point.ToString());
	r.PushStatisticText(std::string("Projected Point : ") + projectedPoint.ToString());
	r.PushStatisticText(std::string("Distance : ") + std::to_string(distance));
}
```