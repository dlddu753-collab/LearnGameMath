# [2026.05.03] 내적을 활용한 목표물 감지

## 핵심 개념
- 사용자 입력에 실시간으로 반응하는 콘텐츠를 만들기 위해서는 가상의 구조를 빠르게 분석하고 가상 공간에 놓인 물체가 어떤 상황인지 측정할수 있는 방법이 제공돼야 한다.
대표적인 방법이 **벡터의 내적**, **벡터의 외적**이다.

## 수학적 개념
- 벡터의 내적 : 같은 차원의 두 벡터가 주어졌을 때, 벡터를 구성하는 각 성분을 곱한 후 이들을 더해 스칼라를 만들어내는 연산이다.
>-  2차원 벡터의 내적을 계산하는 수식  
> $\vec{u} = (a,b)$  
> $\vec{v} = (c,d)$  
> $\vec{u} \cdot \vec{v} = a \cdot c + b \cdot d$
>- 내적의 성질
> 1. 내적은 스칼라 곱셈과 덧셈으로 구성되어 있으므로 교환법칙이 성립한다  
> $\vec{u} \cdot \vec{v} = \vec{v} \cdot \vec{u}$
> 2. 결과가 스칼라로 나오는 성질로 인해 결합법칙 성립하지 않는다.  
> $\vec{u} \cdot (\vec{v} \cdot \vec{w}) \neq (\vec{u} \cdot \vec{v}) \cdot \vec{w}$
> 3. 내적은 덧셈에 대한 분배법칙이 성립한다  
> $\vec{w} \cdot (\vec{u} + \vec{v}) = \vec{u} \cdot \vec{w} + \vec{v} \cdot \vec{w}$
> 4. 같은 벡터를 내적하면 벡터의 크기를 제곱한 결과가 나온다.  
> $\vec{v} \cdot \vec{v} = |\vec{v}|^2$ <- 벡터의 크기정의
- 내적과 삼각함수의 관계 : 벡터의 내적은 두 벡터의 사잇각에 대한 $cos$ 함수와 비례하는 특징을 가진다.
> - 삼각형 점의 좌표  
> $B = (0, 0)$  
> $C = (|\vec{a}|, 0)$  
> $A = (|\vec{c}|cos\beta, |\vec{c}|sin\beta)$  
> - 삼각형 변을 구성하는 벡터   
> $\vec{a} = C - B = (\vec{a}, 0)$  
> $\vec{c} = A -B = (|\vec{c}|cos\beta, |\vec{c}|sin\beta)$  
> $\vec{b} = C - A = (|\vec{a}| - |\vec{c}|cos\beta, 0 - |\vec{c}|sin\beta)$  
> 1. $\begin{aligned} \ |\vec{b}|^2&=(|\vec{a}| - |\vec{c}cos\theta|)^2 + |\vec{c}|^2sin^2\theta \\ &= |\vec{a}|^2 + |\vec{c}|^2 - 2|\vec{a}||\vec{c}|cos\theta :1번\end{aligned}$  
> 2. $|\vec{b}|^2 = \vec{b} \cdot \vec{b} = |\vec{a}|^2 + |\vec{c}|^2 -2\vec{a} \cdot \vec{c}:2번$  
> 3. $|\vec{a}|^2 + |\vec{c}|^2 -2\vec{a} \cdot \vec{c} = |\vec{a}|^2 + |\vec{c}|^2 - 2|\vec{a}||\vec{c}|cos\theta$  
> 4. $\vec{a} \cdot \vec{c} = |\vec{a}||\vec{c}|cos\theta$ : 1번과 2번 동일한 값이므로 왼쪽
식 성립  
> 5. $\vec{u} \cdot \vec{v} = cos\theta$ : 두 벡터의 크기가 1이면 두 벡터의 내적은 $cos$함수가 된다.
> - 두 벡터의 직교  
> 1. $(내적 값 0) = (cos함수 값 0) = 90°(270°,-90°)$  
> 2. 두 벡터의 내적이 0이면 두 벡터는 직교한다  
> -> $(cos\theta,sin\theta)$ 와 $(-sin\theta, cos\theta)$ 두 기저벡터의 내적은 항상 0이 나온다
- 행혈의 곱셈을 내적으로 표현 : 행렬의 곱셈 연산은 내적으로 표현 가능하다
> - 행렬과 벡터의 곱셈  
> $\begin{bmatrix}ax + by \\ cx + dy\end{bmatrix} = \begin{bmatrix}(a,b) \cdot (x,y) \\(c,d) \cdot (x,y)\end{bmatrix}\newline$  
> - 행렬의 곱셈  
> $\begin{bmatrix}a & b \\ c & d\end{bmatrix}\begin{bmatrix}e & f \\ g & h\end{bmatrix} = \begin{bmatrix}ae + bg & af + bh \\ ce + dg & cf + dh\end{bmatrix}=\begin{bmatrix}(a,b) \cdot (e,g) & (a,b) \cdot (f,h) \\ (c,d) \cdot (e,g) & (c,d) \cdot (f,h)\end{bmatrix}$
- 직교 행렬 : 직교행렬은 정방행렬을 구성하는  모든 행벡터,열벡터의 크기가 1이고 서로 직교하는 행렬이다
> 1. 직교행렬의 정의에 의해 벡터 (a,b)와 (c,d)는 서로 직교하므로 두 벡터의 내적은 0이 나온다.  
> 2. 직교행렬의 정의에 의해 행벡터와 열벡터의 크기가 1이므로, 자신을 내적한 결과는 1이 된다. 
> $\begin{bmatrix}(a,b) \cdot (a,b) & (a,b) \cdot (c,d) \\ (c,d) \cdot (a,b) & (c,d) \cdot (c,d) \end{bmatrix} =\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} = I\\$
> 3. 회전 변환행렬도 각 행벡터와 열벡터의 크기가 1이고 서로 직교하므로 직교행렬이다.  
> $R_\theta = \begin{bmatrix}cos \theta & -sin \theta \\ sin\theta & cos\theta\end{bmatrix}\\$  
> $R_\theta^T \cdot R_\theta = \begin{bmatrix}cos^2\theta + sin^2\theta & 0 \\ 0 & cos^2\theta + sin^2 \theta\end{bmatrix} = \begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} = I$  
- 강체 변환 : 선형 변환 중 물체의 형태가 그대로 유지되는 선형 변환
> 1. 변화된 기저벡터의 크기는 모두 1이어야 한다.
> 2. 모든 기저벡터는 서로 직교해야 한다.
> 3. 행렬식의 값이 1이어야 한다.
> - 회전 변환은 물체의 형태를 그대로 유지시켜주는 강체 변환이다.
- 시야 판별 : 캐릭터 시야각 영역에 목표물이 존재하는지 내적을 활용해 파악할수있다.
> - 앞뒤 판별 : 두 벡터의 내적의 부호를 통해 쉽게 파악할 수 있다.  
>  (벡터의 크기값은 언제나 양수이므로 벡터 내적의 부호는 $cos$함수가 결정)  
>  ($cos$함수는 $(-90°,90°)$영역에서 양의 부호를 가지고 -90°,90°에서는 0, 그 이외 범위에서는 음의 부호를 가진다.)  
> 1. 두 벡터 내적의 결과가 양수 : 캐릭터 앞에 목표물이 있다.
> 2. 두 벡터 내적의 결과가 음수 : 캐릭터 뒤에 목표물이 있다.
> 3. 두 벡터 내적의 결과가 0 : 캐릭터 바로 옆에 목표물이 있다.
> - 시야 판별 : 캐릭터에 시야각이라는 특성을 부여한뒤, 캐릭터의 시선 방향과 목표물의 사잇각을 내적을 통해 구한뒤 시야각에 대한 비교를 통해 목표물이 시야에 탐지 되는지 판별될 것이다.  
> ($cos$함수는 $[0°,180°]$ 범위에서 각이 커질수록 값이 작아지는 성질을 지닌다.)  
> 1. 시야각을 절반 나눈 각의 $cos\frac{\beta}{2}$를 미리 계산해둔다.  
> 2. 캐릭터의 시선 벡터를 구하고 크기를 1로 정규화. $\vec{f}$
> 3. 캐릭터에서 목표물로 향하는 벡터도 정규화. $\vec{v}$
> 4. 내적 $\vec{f} \cdot \vec{v}$ 의 계산 결과는 $cosa$ 가 된다. 앞에 계산해 둔 $cos\frac{\beta}{2}$값과 비교한다.  
> -> $\vec{f} \cdot \vec{v} \geq cos\frac{\beta}{2}$ : 목표물이 시야 범위 안에 있음  
> -> $\vec{f} \cdot \vec{v} < cos\frac{\beta}{2}$ : 목표물이 시야 범위 밖에 있음
## 게임 적용 
- 캐릭터 시야각 영역에 목표물이 존재하는지 내적을 활용해 파악할수 있다
 ## 코드예제
```c++
// 게임 로직을 담당하는 함수
void SoftRenderer::Update2D(float InDeltaSeconds)
{
	// 게임 로직에서 사용하는 모듈 내 주요 레퍼런스
	auto& g = Get2DGameEngine();
	const InputManager& input = g.GetInputManager();

	// 게임 로직의 로컬 변수
	static float moveSpeed = 100.f;
	// 목표물이 랜덤으로 이동하는 데 사용할 함수 선언.
	static std::random_device rd;
	// 변수 rd,mt 균등 분포에 사용되는 난수를 사용하기 위해 선언한 변수
	static std::mt19937 mt(rd());
	// 랜덤 함수는 x축으로 [-300,300] 범위 내 값을 생성
	static std::uniform_real_distribution<float> randomPosX(-300.f, 300.f);
	// 랜덤 함수는 y축으로 [-200,200] 범위 내 값을 생성
	static std::uniform_real_distribution<float> randomPosY(-200.f, 200.f);
	static float duration = 3.f;
	static float elapsedTime = 0.f;
	//  목표물 시작지점 설정
	static Vector2 targetStart = targetPosition;
	// 랜덤함수를 사용해 목표물이 이동할 목표 지점 설정
	static Vector2 targetDestination = Vector2(randomPosX(mt), randomPosY(mt));

	// 시야각의 cos 값은 최초 1회만 계산해 보관
	// 플레이어 시야각의 절반값에 대한 cos함수 값 구하고 변수에 저장
	static float halfFovCos = cosf(Math::Deg2Rad(fovAngle * 0.5f));
	// 프레임 시간을 더해 경과 시간 변수 elapsedTime 값을 누적하되 최종 경과 시간 duration을 넘지 않도록 clamp 함수 사용
	elapsedTime = Math::Clamp(elapsedTime + InDeltaSeconds, 0.f, duration);

	// 경과 시간이 목표값 duration에 다다르면 랜덤함수를 사용해 목표물의 위치 값을 재설정하고 경과 시간 값을 초기화
	if (elapsedTime == duration)
	{
		targetStart = targetDestination;
		targetPosition = targetDestination;
		targetDestination = Vector2(randomPosX(mt), randomPosY(mt));

		elapsedTime = 0.f;
	}
	else // 비율에 따라 목표지점까지 선형보간하면서 이동
	{
		float ratio = elapsedTime / duration;
		targetPosition = Vector2(
			Math::Lerp(targetStart.X, targetDestination.X, ratio),
			Math::Lerp(targetStart.Y, targetDestination.Y, ratio)
		);
	}

	Vector2 inputVector = Vector2(input.GetAxis(InputAxis::XAxis), input.GetAxis(InputAxis::YAxis)).GetNormalize();
	Vector2 deltaPosition = inputVector * moveSpeed * InDeltaSeconds;
	// 플레이어 시야각 벡터. 플레이어는 항상 Y축만 바라보도록 설정
	Vector2 f = Vector2::UnitY;
	// 목표물 위치에서 플레이어 위치를 빼 플레이어에서 목표물로 향하는 벡터를 생성하고 이를 정규환 시킴
	Vector2 v = (targetPosition - playerPosition).GetNormalize();
	// 물체의 최종 상태 설정
	// 내적 값을 비교하여 내적 값이 hlafFovCos보다 크면 시야 안에 들어와 있는 것이므로 플레이어,목표물 색상 붉은색으로 설정
	if (v.Dot(f) >= halfFovCos)
	{
		playerColor = LinearColor::Red;
		targetColor = LinearColor::Red;
	}
	else
	{
		playerColor = LinearColor::Gray;
		targetColor = LinearColor::Blue;
	}
	playerPosition += deltaPosition;
}
```