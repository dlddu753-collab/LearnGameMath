# [2026.05.04] 와이어 프레임

## 핵심 개념
- 아핀 결합식으로 세 점을 결합하여 삼각형을 만들고 삼각형을 중심으로 물체의 외형을 표현하는 방법인 와이어프레임에 대해 알아보고자 한다.


## 수학적 개념
- 컨벡스 결합 : 아핀 결합에서 모든 스칼라 값을 [0,1] 범위로 한정한 결합
> 1. 세 점 $P_1,P_2,P_3$ 로 결합하는 경우 세 스칼라의 합은 1이 되어야 함(아핀 결합 유지 조건)  
> $P^\prime = s \cdot P_1 + t \cdot P_2 + (1-s-t) \cdot P_3$  
> -필수 조건-  
> $0 \le s \le 1$  
> $0 \le t \le 1$  
> $0 \le 1-s-t \le 1$ (즉, $s + t \le 1$)  
> 2. 점을 빼는 수식으로 전개  
> $(P^\prime - P_3) = s(P_1-P_3) + t(P_2 - P_3)$  
> 3. 각 괄호를 벡터로 치환  
> $\vec{w} = s \cdot \vec{u} + t \cdot \vec{v}$  
> ![세 점의 아핀 결합](<img width="366" height="234" alt="image" src="https://github.com/user-attachments/assets/ccf8ed64-e348-4ab2-8da4-a31e9c9d9b69" />)  
> $\displaystyle\sum_{i=0}^{n}c_i \cdot P_i (s.t. \displaystyle\sum_{i=0}^{n}c_i =1, 0 \leq c_i \leq 1)$  
> - 네 점의 아핀 결합  
> 세 벡터가 선형 독립 관계를 이룬다면 3차원 공간의 모든 벡터를 생성할 수 있으므로 스칼라의 값을 [0,1]로 범위를 제한한다면 삼각뿔 영역이 형성 될것이다.  
> -> $\vec{x} = a \cdot \vec{u} + b \cdot \vec{v} + c \cdot \vec{w}$
- 컨벡스 영역 : 선분이나 삼각형처럼 컨벡스 결합으로 만든 영역. 컨벡스는 '볼록한'이라는 뜻한다.
> 영역 내 임의의 두 점을 연결한 선분을 만들었을때 그 선분은 언제나 해당 컨벡스 영역 안에 속한다
> 컨벡스가 컨케이브 물체보다 충돌 검사 속도가 매우 빠름
- 컨케이브 : 컨벡스의 반대인 오목한 모양을 뜻한다.
> 컨케이브 영역은 임의의 두 점을 이은 선분이 영역 밖으로 벗어나는 경우가 발생한다
- 메시 : 삼각형을 중심으로 물체에 관련된 정보를 기록한 데이터
> 메시는 정점(Vertex)들이 모인 데이터라고 할수 있다.
- 정점(Vertex) : 메시의 삼각형을 이루는 각 점의 위치정보와 색상이나 방향과 같은 부가 정보를 묶은 특별한 점
- 정점 버퍼(Vertex Buffer) : GPU에게 전달할 정점의 데이터(위치, 색상, UV 등)를 담는 연속적인 메모리 블록.
> 
- 인덱스 버퍼(Index Buffer) : 정점 버퍼의 정보만으로는 어떤 세 점이 모여 삼각형을 구성하는지 알수 없으니, 삼각형을 구성하는 정점의 순번만 기록한 배열을 별도로 만든것 
> - 인덱스 버퍼 장점
> 1. 메모리 절약 : 4개의 정점만으로 삼각형 2개 표현(원래라면 6개의 정점 필요)
> 2. 포스트-트랜스폼 캐시 : GPU는 같은 인덱스의 정점이 들어오면 이미 계산된 좌표를 재사용합니다. 중복연산 하지 않도록 도와줌.
- 와이어프레임 : 삼각형의 외곽선만 그려 메시를 표현하는 방법

## 게임 적용 
- 다수의 삼각형으로 구성된 메시로 게임속 물체의 형상을 표현 할수 있다.

 ## 코드예제
```c++
// 렌더링 로직을 담당하는 함수
void SoftRenderer::Render2D()
{
	// 렌더링 로직에서 사용하는 모듈 내 주요 레퍼런스
	auto& r = GetRenderer();
	const auto& g = Get2DGameEngine();

	// 배경에 격자 그리기
	DrawGizmo2D();

	// 메시 데이터의 선언
	// 와이어프레임으로 그릴 정사각형의 절반 크기로 지정
	static constexpr float squarHalfSize = 0.5f;
	// 정사각형을 구성하는 전체 정점의 수. 정사각형은 삼각형 2개로 구성되지만 빗변을 공유하므로 4개
	static constexpr size_t vertexCount = 4;
	// 정사각형을 구성하는 삼각형 수
	static constexpr size_t triangleCount = 2;

	// 메시를 구성하는 정점 배열과 인덱스 배열의 생성
	static constexpr std::array<Vertex2D, vertexCount> rawVertices =
	{
		// 정점 버퍼 생성
		Vertex2D(Vector2(-squarHalfSize, -squarHalfSize)),
		Vertex2D(Vector2(-squarHalfSize, squarHalfSize)),
		Vertex2D(Vector2(squarHalfSize, squarHalfSize)),
		Vertex2D(Vector2(squarHalfSize, -squarHalfSize)),

	};

	static constexpr std::array<size_t, triangleCount * 3> indices =
	{
		// 인덱스 버퍼 생성
		0,1,2,
		0,2,3
	};

	// 아핀 변환 행렬 ( 크기 ) 
	Vector3 sBasis1(currentScale, 0.f, 0.f);
	Vector3 sBasis2(0.f, currentScale, 0.f);
	Vector3 sBasis3 = Vector3::UnitZ;
	Matrix3x3 sMatrix(sBasis1, sBasis2, sBasis3);

	// 아핀 변환 행렬 ( 회전 ) 
	float sin = 0.f, cos = 0.f;
	Math::GetSinCos(sin, cos, currentDegree);
	Vector3 rBasis1(cos, sin, 0.f);
	Vector3 rBasis2(-sin, cos, 0.f);
	Vector3 rBasis3 = Vector3::UnitZ;
	Matrix3x3 rMatrix(rBasis1, rBasis2, rBasis3);

	// 아핀 변환 행렬 ( 이동 ) 
	Vector3 tBasis1 = Vector3::UnitX;
	Vector3 tBasis2 = Vector3::UnitY;
	Vector3 tBasis3(currentPosition.X, currentPosition.Y, 1.f);
	Matrix3x3 tMatrix(tBasis1, tBasis2, tBasis3);

	// 모든 아핀 변환을 곱한 합성 행렬. 크기-회전-이동 순으로 적용
	Matrix3x3 finalMatrix = tMatrix * rMatrix * sMatrix;

	// 행렬을 적용한 메시 정보를 사용해 물체를 렌더링
	// 변환을 적용해 렌더링에 사용할 정점 버퍼를 생성
	static std::vector<Vertex2D> vertices(vertexCount);
	// 각 정점별로 행렬 곱을 적용한 결과를 보관
	for (size_t vi = 0; vi < vertexCount; ++vi)
	{
		vertices[vi].Position = finalMatrix * rawVertices[vi].Position;
	}

	// 변환된 정점을 잇는 선 그리기
	for (size_t ti = 0; ti < triangleCount; ++ti)
	{	// 인덱스 버퍼는 항상 3의 배수로 구성, 각 삼각형이 시작되는 인덱스를 계산
		size_t bi = ti * 3;
		// 인덱스 버퍼로부터 삼각형을 추출해 하나씩 처리
		r.DrawLine(vertices[indices[bi]].Position, vertices[indices[bi + 1]].Position, _WireframeColor);
		r.DrawLine(vertices[indices[bi]].Position, vertices[indices[bi + 2]].Position, _WireframeColor);
		r.DrawLine(vertices[indices[bi + 1]].Position, vertices[indices[bi + 2]].Position, _WireframeColor);
	}
```